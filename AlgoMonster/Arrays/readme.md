
ARRAY / STRING problems
│
├── Non-contiguous?
│     └── ❌ Not these patterns (DP / Hash / Recursion)
│
└── Contiguous range?
      │
      ├── Sorted or comparing ends?
      │     └── Two Pointers
      │
      └── Subarray / substring?
            │
            ├── Window size fixed?
            │     └── Sliding Window (fixed)
            │
            ├── Can condition be maintained monotonically?
            │     └── Sliding Window (variable)
            │
            └── Negatives / counting / exact sums?
                  └── Prefix Sum (+ HashMap)


